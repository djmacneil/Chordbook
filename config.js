/* config.js — deployment-wide defaults for this ChordBook instance.
   Edit this before hosting a public/shared copy so every visitor gets your
   proxy automatically, with no Settings step required on their end.
   Leave defaultProxyUrl empty ('') for a private, personal instance where
   each person connects their own Dropbox account instead. */

window.CHORDBOOK_CONFIG = {
  defaultProxyUrl: '', // e.g. 'https://chordbook-proxy.yourname.workers.dev'
};
